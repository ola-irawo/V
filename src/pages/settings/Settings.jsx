import React from 'react'
import "./settings.css"
import RenderSettings from '../../features/settings/renderSettings/RenderSettings'

const Settings = () => {
  return (
    <main className="settings">
      <RenderSettings />
    </main>
  )
}

export default Settings
